import { FilterProgress2Pipe } from './filter-progress2.pipe';

describe('FilterProgress2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProgress2Pipe();
    expect(pipe).toBeTruthy();
  });
});
